@entries.map do |entry|
  {
    user_id: entry.user_id,
    id: entry.id,
    date: entry.date,
    location: entry.location,
    weather: entry.weather,
    notes: entry.notes,
    fish: entry.fish.all,
  }
end
